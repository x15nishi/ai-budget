streamlit
pandas
matplotlib
langchain
langchain-core
langchain-google-genai
python-dotenv
pillow
pytesseract
openpyxl

# NOTE: pytesseract is just a Python wrapper - it needs the actual Tesseract OCR
# engine installed on the machine separately (it's not a pip-installable binary):
#   Ubuntu/Debian:  sudo apt-get install tesseract-ocr
#   macOS:          brew install tesseract
#   Windows:        https://github.com/UB-Mannheim/tesseract/wiki
# openpyxl is needed so pandas can read .xlsx bank statement exports.
